export default function Toolbar() {
    const style = {
        width: '100%',
        background: 'red',
        height: '20px',
        position: 'fixed',
        top: 0,
        zIndex: 50

    };

    return <div style={style}></div>;
}
